(when something
    (something-else))

(filter even?
  (range 1 10))

(filter
  even?
  (range 1 10))

(let [thing1 "some stuff"
  thing2 "other stuff"]
  {:thing1 thing1
  :thing2 thing2})

( let [x 3
    y 4]
  (+ (* x x
  )(* y y)
  ))

(def x #"\n")
